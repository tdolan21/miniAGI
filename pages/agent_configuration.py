import streamlit as st
from dotenv import load_dotenv

load_dotenv()

st.title("miniAGI agent configurations :computer:")
st.info("The configurations made on this page are shared across all pages. This means that if you change the configurations here, they will be reflected on all pages.")

